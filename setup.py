from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1",
    author = "Vaibhav Yaramwar",
    description = "its a wine Q package",
    packages = find_packages(),
    license = "MIT"

)
